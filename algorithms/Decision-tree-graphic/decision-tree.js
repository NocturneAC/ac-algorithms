function transportationOptions() {
    // Write code here to make the decision tree into an algorithm!
    // what's the weather? input button
    // weather? overall grandparent
    let weather;
    let time;
    let hungry;
    let bus;
    let walk;
    let transportation;
      // if weather === rain, return bus
      // else if weather === cloudy, return text input
        // is hungry? radio select (true/false)
          // is hungry == true, return walk
          // is hungry == false, return bus
      // else if weather === sunny, return text input
        // how much time, >30 mins or <30mins? radio select (t/f)
          // time == >30 mins, return walk
          // time == <30 mins, return bus

  }

// TERMINAL GIT PUSH INSTRUCTIONS:
// 1. cd into the repo
// 2. git add .
// 3. Git commit -m “commit message”
// 4. Git push origin main